import 'package:flutter/material.dart';
import 'package:unity26_app_v1/screens/is_assistant/components/body.dart';
class IsAssistantScreen extends StatelessWidget {
  const IsAssistantScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(),
    );
  }
}