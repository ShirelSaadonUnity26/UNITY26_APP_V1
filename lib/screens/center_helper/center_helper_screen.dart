import 'package:flutter/material.dart';

class CenterHelperScreen extends StatelessWidget {
  const CenterHelperScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
